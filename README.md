# Fitness Dashboard — Tailwind + React + Framer Motion

## Run locally
1. npm install
2. npm run dev
3. Open http://localhost:5173

## Build & Deploy
- Build: `npm run build`
- Deploy: connect repo to Vercel, build command `npm run build`, output `dist`.
